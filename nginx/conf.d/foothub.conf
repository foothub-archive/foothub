upstream backend {
  ip_hash;
  server auth:8000;
}

# portal
server {
  listen 80;
  server_name localhost;

  location /static/ {
    autoindex on;
    root /etc/nginx/html;
  }

  location /api/auth/ {
    proxy_pass http://auth/;
  }

}
